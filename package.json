{
  "name": "leaving-earth",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "generate:icons": "node scripts/generate-placeholder-icons.mjs",
    "generate:components": "node scripts/generate-component-placeholders.mjs",
    "generate:assets": "npm run generate:icons && npm run generate:components"
  },
  "dependencies": {
    "framer-motion": "^12.42.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "zustand": "^5.0.14"
  },
  "devDependencies": {
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^4.7.0",
    "typescript": "^6.0.3",
    "vite": "^5.0.0",
    "vitest": "^4.1.9"
  }
}
